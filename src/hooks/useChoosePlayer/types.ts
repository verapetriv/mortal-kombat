export interface AllCoordinatesType {
  [key: number]: { x: number; y: number };
}

export enum Arrows {
  ArrowUp = 'ArrowUp',
  ArrowDown = 'ArrowDown',
  ArrowLeft = 'ArrowLeft',
  ArrowRight = 'ArrowRight',
}
